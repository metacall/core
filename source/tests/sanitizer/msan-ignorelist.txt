# The rules in this file are only applied at compile time. If you can modify the
# source in question, consider function attributes to disable instrumentation.
#
# Please think twice before you add or remove these rules.

[memory]

# GoogleTest is ignored because it is built with MSan support
# but its internal implementation triggers false positives. The googletest
# library is instrumented separately with the correct MSan flags and ignorelist
# via InstallGTest.cmake, so we suppress the googletest sources here to avoid
# noise from the testing framework itself.
src:*/googletest/*
fun:testing::*
