#
#	NodeJS project generator by Parra Studios
#	Copyright (C) 2016 - 2019 Vicente Eduardo Ferrer Garcia <vic798@gmail.com>
#
#	Generates a nodejs project embedded into CMake
#

# Check if this loader is enabled
if(NOT OPTION_BUILD_SCRIPTS OR NOT OPTION_BUILD_SCRIPTS_JS)
    return()
endif()

#
# External dependencies
#

# TODO

#find_package(NodeJS)

# Target name
set(target @PACKAGE_NAME@)

# Exit here if required dependencies are not met
message(STATUS "Script ${target}")

# Custom nodejs section
if(NODEJS_FOUND)

	# TODO

	# Set custom setup command
	#add_custom_command(OUTPUT  "@PACKAGE_OUTPUT@"
	#				   COMMAND "${NODEJS_EXECUTABLE}" "@PACKAGE_SETUP_NODEJS@" build
	#				   COMMAND "${CMAKE_COMMAND}" -E touch "@PACKAGE_OUTPUT@"
	#				   DEPENDS "@PACKAGE_DEPS@")
	#
	# Deploy generated config file
	#install(CODE   "execute_process(COMMAND ${NODEJS_EXECUTABLE} @PACKAGE_SETUP_NODEJS@ install)"
	#	COMPONENT   runtime
	#)
endif()
