name: Build and Push Docker Image

on:
  workflow_dispatch:
  push:
    branches:
      - master
    tags:
      - 'v*.*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v3

      - name: Run a sample command
        run: echo "Building Docker image..."

  push_manifest:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout the code
        uses: actions/checkout@v3

      - name: Run a sample command
        run: echo "Pushing Docker manifest..."
