TODO:
	loader_id -> divide into
		1) loader_extension/naming/whatever

		2) a more general approach about language id

	implement context as a base class
	move loader.h & loader.c into loader/.
	move call.h & call.c into call/.
	implement a top module (metacall with a group of contexts)


	do a basic api running only python ---> (almost done)
	do a basic test with that api
	remove this file
